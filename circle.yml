machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t vidsyhq/go-base .

test:
  override:
    - echo "Nothing..."

deployment:
  hub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push vidsyhq/go-base
